# OpenMP

Михаил Курносов (https://www.mkurnosov.net/)

## Задание 1: умножение матрицы на вектор
Используя семинар 1 Михаила Курносова, реализовать
 __умножения матрицы на вектор с параллельной инициализацией массивов.__   
 Для элементов массива используйте тип double.
 Размеры матриц: 20000x20000, 40000x40000.

## Задание 2: Интегрирование
Используя семинар 2 Михаила Курносова, реализовать
 __численное интегрирование, с использованием «#pragma omp atomic» и локальной переменной.__   
 nsteps = 40 000 000.

## Общие требования
- 1,2,4,7,8,16,20,40 потоков.

Описать вычислительный узел:
1. наименование и краткая характеристика CPU (lscpu),
2. наименование сервера (cat /sys/devices/virtual/dmi/id/product_name),
3. сколько NUMA node (numactl --hardware), сколько памяти у каждой ноды,
4. операционная система (cat /etc/os-release).

Построить график ускорения в зависимости от количества потоков:
* Написать вывод о маштабируемости.
* Результат сохранить в формате pdf, и прикрепить в гугл класс.  

<u>Дополнительно</u> (по желанию):  
* Привязать потоки к ноде или конкретным ядрам,
и сравнить результат с предыдущим (без привязки).

## Задание 3: Лабораторная работа №2
__«Параллельная реализация решения системы линейных алгебраических уравнений с помощью OpenMP»__

### Постановка задачи
- Элементы главной диагонали матрицы A равны 2.0, остальные равны 1.0.
-  Все элементы вектора b равны N+1.
-  В этом случае решением системы будет - вектор, элементы которого равны 1.0.
-  Начальные значения элементов вектора x можно взять равными 0.

### Шаг 1: Способы реализации
Реализовать итерационный алгоритм решения системы линейных
алгебраических уравнений вида Ax=b, и распараллелить с помощью
OpenMP.
Реализовать два варианта программы:
* Вариант 1: для каждого распараллеливаемого цикла создается
отдельная параллельная секция `#pragma omp parallel for`
* Вариант 2: создается одна параллельная секция `#pragma omp
parallel`, охватывающая весь итерационный алгоритм.

Уделить внимание тому, чтобы при запуске программы на различном
числе OpenMP-потоков решалась одна и та же задача (исходные
данные заполнялись одинаковым образом).

### Шаг 2: Замер времени
Замерить время работы двух вариантов программы при использовании
различного числа процессорных ядер: от 1 до числа доступных в узле.
Построить графики зависимости времени работы программы,
ускорения и эффективности распараллеливания от числа используемых
ядер. Исходные данные и параметры задачи подобрать таким образом,
чтобы решение задачи на одном ядре занимало не менее 30 секунд.

### Шаг 3
Провести исследование на определение оптимальных параметров
`#pragma omp for schedule(...)` при некотором фиксированном размере
задачи и количестве потоков.

### Шаг 4
На основании полученных результатов сделать вывод о
целесообразности использования первого или второго варианта
программы.


## Пояснения
Для выполнения задач смотреть исходники
1 - dgemv.c
2 - integral.c
3 - Самостоятельно
