# 8. CUDA programming

## Задание (Отличия от 7-го задания)
- Перенести программу на GPU используя __CUDA__..
- Операцию редукции (вычисление максимального значения ошибки)
  реализовать с использованием __библиотеки CUB__,
  [на уровне блока](https://nvidia.github.io/cccl/cub/api/classcub_1_1BlockReduce.html#).
- Применить _cuda graph_, для итераций между подсчётами ошибки.

- Сравнить скорость работы для разных размеров сеток на графическом
  процессоре с предыдущей реализацией на OpenACC.
- Профилирование через “Nsight Systems”.


## Отличие воплощения от task7.cpp
#### 1) Производительность
__Код ужасно быстрый!__  
* `cudaStreamSynchronize` Всего `47.1%` времени!
* А `CUDA memcpy` = 2. Каких-то 2!

#### 2) Архитектура
Обратный сдвиг к `task6.cpp`: Общая матрица.


## Теория CUDA
Для разделения на чистый `.cpp` (host-код) и `.cu` (device-код) следовать принципу:
  **в `.cu` выносится всё, что требует `nvcc` (ядра, запуски, CUB, управление памятью GPU, графы), а в `.cpp` остаётся только логика, не зависящая от CUDA, – инициализация границ, парсинг аргументов, вызов функции-обёртки и вывод результатов.**
1. Ссылки на __список всех операций__
2. Разбор имеющихся: `...`
3. Общая работа с ...
